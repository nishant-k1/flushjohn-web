import React from "react";

const ModalBackground = () => {
  return (
    <div
      style={{
        height: "100%",
        width: "100%",
        background: "rgba(0, 0, 0, 0.5)",
        position: "absolute",
        overflowY: "hidden",
      }}
    />
  );
};

export default ModalBackground;
