import React from "react";

const ModalOverlay = () => {
  return (
    <div
      style={{
        height: "100%",
        width: "100%",
        background: "rgba(0, 0, 0, 0.5)",
        position: "fixed",
      }}
    />
  );
};

export default ModalOverlay;
