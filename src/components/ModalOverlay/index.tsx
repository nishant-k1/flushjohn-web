import React from "react";

const ModalOverlay = () => {
  return (
    <div
      style={{
        height: "100%",
        width: "100%",
        background: "var(--black-alpha-50)",
        position: "fixed",
      }}
    />
  );
};

export default ModalOverlay;
